package ai.scads.kg.core.model

class DDIMessage {

  // is a graph itself
  // contains a relevant infomrations is extensible
  // a tool that matches this graph will use it to process its data
  // we need a registry that is disributed, like in SPARK
  // then we can add components in this fasion
  // 1. the main part is just the distribution service
  // 2. is the construction of the meta kg
  // 3. is the integration and eval of tools
  // 3.X tool definition (manual, auto), 
}
