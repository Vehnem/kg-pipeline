import subprocess

# Path to the LIMES executable
limes_executable = "/path/to/limes"

# Path to the LIMES configuration file
limes_config = "/path/to/limes_config.xml"

# Run LIMES
subprocess.run([limes_executable, limes_config])
